package Deck.game;

import Deck.Card;

public class HandManager {
  private Card[] cards;

  public HandManager(Card[] cards) {
    this.cards = cards;
  }

  public boolean isFullHouse() {
    // calculate this.cards
    return false;
  }
}